# Optional: enables the Anthropic LLM provider for /api/ask.
# If unset, the app probes for a local Ollama instance (localhost:11434), then
# falls back to a "no LLM configured" state — the app runs fine without either.
ANTHROPIC_API_KEY=
